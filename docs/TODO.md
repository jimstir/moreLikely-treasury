# TODO

- Create a dedicated Sepolia private wallet for testnet deployment.
- Add the wallet private key to `.env` as `OWNER_PRIVATE_KEY`.
- Verify the Hardhat Sepolia deployment script and confirm it uses the same contracts as the application.
- Ensure the testnet deployment flow uses `npx hardhat run --network sepolia` or `npx hardhat test --network sepolia` as appropriate.
